<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.DetailsActivity_sizes">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/details_layout_height_0</item>
        <item name="android:layout_weight">40</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="AppTheme.DetailsActivity_tv" parent="AppTheme.DetailsActivity_sizes">
        <item name="android:layout_width">@dimen/details_size_width_textView_200</item>
        <item name="android:layout_gravity">end</item>
        <item name="android:layout_marginTop">@dimen/spacing_small_8</item>
        <item name="android:layout_weight">10</item>
        <item name="android:background">@drawable/shape_tv</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="AppTheme.DetailsActivity_tvSummary" parent="AppTheme.DetailsActivity_sizes">
        <item name="android:layout_marginTop">@dimen/spacing_small_8</item>
        <item name="android:layout_weight">30</item>
        <item name="android:background">@drawable/shape_tv</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
